# AGENTS.md

给在本仓库工作的 AI agent 的入口说明。**不绑定特定宿主**——本文与 skill 正文都是纯
markdown 与相对仓库根的路径，任何能读文件、能把 markdown 当指令执行的 agent 都适用。

## 这是什么

明日方舟**基建**知识库，外加一个基于它的问答 skill。

- 语料是专门研究基建的玩家（公孙长乐）沉淀的**定性判断**——哪个组合更强、为什么这样搭、缺人怎么退
- skill 把语料和全量干员基建技能数据接起来，用于回答玩家提问
- **只覆盖基建**：排班、干员搭配、体系选择、设施效率、布局、缺人降级、基建机制。
  不涉及战斗、关卡、抽卡、战斗向练度规划

---

## 要回答基建问题

**先完整读 `.claude/skills/gongsun-changle/SKILL.md`**，并严格按它的回答管线、约束检查清单
和计算政策执行。不要跳过它凭自己的印象作答——这套语料专业性很强，且模型自身的方舟知识
既过时也不可靠。

如果宿主支持 skill / 规则机制，把 `gongsun-changle/` 装进去，让它自动加载。

### 三条最容易出错的，摘在这里

即使你只读了本文没读 SKILL.md，这三条也请遵守。它们是三个已验证的高频错误来源：

1. **派系条件必须查表。** 凡是技能写「每有 1 名 XX 派系干员」，去
   `references/类别.md` 查官方成员表，**不许凭名字推断**。异格干员经常不在本体的派系里——
   比如维娜·维多利亚是推进之王的异格，但**不属于**格拉斯哥帮。语料里就有一处因此算错的推导。

2. **技能名要过等价组。** 游戏里存在大量显示名不同、效果描述逐字相同的技能，玩家用一个
   俗称统指。查 `references/技能等价组.md`（82 组），否则按字面技能名检索会漏掉大部分持有者。
   典型：「裁缝β」的持有者是 4 人，只查「裁缝·β」这个名字只能查到 2 人。

3. **区分「解锁」与「提升」。** `references/技能-<设施>.md` 里每条技能都标了解锁条件。
   `精英 N 提升` = **替换**前一档，只算新的；`初始解锁` / `精英 N 解锁` = **新增并存**，两个都算。
   425 名干员里 165 名有升级链，把提升当并存会把数值算重。

### 不确定时

**允许出错，不允许闷头猜。** 名册里查不到的名字就说不知道；截图看不清就问；
参考表没有对应档位就明说。不确定却装作确定的回答读起来最专业，也最难被用户发现。

---

## 要维护仓库

### 三层内容，各自的改法

| 层 | 路径 | 改法 |
|---|---|---|
| **语料**（人的判断） | `docs/**/*.md` | 人工编辑。这是知识库正文 |
| **数据**（游戏客观事实） | `.claude/skills/gongsun-changle/references/` | **不要手改。**改上游数据后重跑脚本 |
| **指南**（人工整理的读法） | `.claude/skills/gongsun-changle/guides/` | 人工编辑 |

两条线互不干扰：**语料改 `docs/`，数据改上游后重跑脚本。**

### 语料边界

- `docs/**/*.md` 是知识库正文，适合进入 RAG 索引
- `meta/templates/` 是写作模板和早期系统提示词，**不是**玩家知识正文
- `语料校对清单.md` 由脚本生成，供人工校对，不入库

### 重新生成 references/

需要 Python 3.10+，以及两个数据仓库与本仓库**互为兄弟目录**：

```
<任意父目录>/
  ├── arknights-base-vault/     本仓库
  ├── RIIC-Web/                 主源：干员目录、基建技能目录、官方术语表
  └── RhodeLogisticsSteward/    补源：roomType / efficiency / targets / 派系归属
```

```bash
python .claude/skills/gongsun-changle/scripts/build_refs.py
```

按上述布局放好就不用带参数；放在别处用 `--workspace <公共父目录>`。目录缺失时脚本会直接
报错，不生成半成品。

### 校对语料

```bash
python .claude/skills/gongsun-changle/scripts/check_corpus.py
```

把 `docs/` 与全量数据逐项对照，在仓库根生成 `语料校对清单.md`：干员名与星级核对、
练度记法、简称合称、技能名与官方数据的对应、**派系归属断言核对**、数值口径不一致、
同效干员漏收。每条都带出处，改完语料重跑即可复查。

### 数据来源

`references/` 由两个上游合并而成，口径差异已在
`.claude/skills/gongsun-changle/references/数据源.md` 中记录（星级起算点、buffId 格式、
`roomType` 推导方式、已知缺口）。改脚本前先读它。
