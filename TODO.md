# TODO

## KnightCode

- [x] 发电站补充无人机机制（充能仅受发电站干员影响、无视上限持续充能、溢满不停工）
- [x] 修复 `[[αβγ三队轮换]]` 死链 → `[[排班轮换策略]]`
- [x] 修复 `[[无人机机制]]` 死链 → `[[发电站机制]]`
- [x] 龙门中枢组注解写入散件干员速查，移除独立文档死链
- [ ] 补全 `docs/5-cli求解器/` — infra-cli 求解器介绍和使用说明

## 公孙长乐

- [ ] 补全 `docs/3-单站组合/` — 介于散件与体系之间的双人/三人固定组合（德狼、能蕾、孑+银灰、多萝西+淬羽赫默等），每篇包含效率锚点、缺人降级路径

---

## 问答 skill 需要的补充语料（公孙）

来源：`.claude/skills/gongsun-changle/` 开发过程中，从真实社区提问里发现的语料缺口。
每项都是**改 prompt 解决不了的**——缺的是内容，不是措辞。
题型与真实提问样本见 `.claude/skills/gongsun-changle/evals/题型.md`。

- [ ] **布局规划与参考产量** — 不同设施数量规划的侧重点，以及各布局的大致参考产量。
      合并了原先设想的两篇（布局对比 + 产能参考），因为参考产量本来就按布局分档。
      现状：252 在全语料只出现 2 次，问到基本只能给降级答案；没有任何参考产量基准。
      已有 3 条真实简报样本可作种子，其中一条已知体系完成度（巫恋核 + 自动化组 + 但书链）。

- [ ] **养成成本与回本参考** — 一篇，两张表：
      - 成本表：`3★ 精0满级 / 精1 1级`、`4★ 精1 1级 / 精2 1级`、`5★ …`、`6★ …`（约 8 行，人工可定）
      - 回本速率表：`等效 10% 效率 → 单位时间回本 XXXX`、`等效 20% → XXXX`、`特殊干员 → XXXX`
      查询路径：干员 → 星级 + 解锁阶段 → 成本；干员 → 效率档 → 速率；一次除法得天数。
      **注意**：基建技能在精 N 的 **1 级**即解锁（数据中 364 条技能门槛为 `elite=2 / level=1`），
      不需要练满级——按满级估成本会大幅高估。

- [ ] **单站组合** `docs/3-单站组合/` — 见上方原条目。优先级建议上调：
      真实提问里「六星一堆但体系核心一个没有」是高频场景，13 篇体系文档对这类用户全部不适用。

## 需要公孙裁决的语料问题

详见仓库根目录 `语料校对清单.md`（由 `check_corpus.py` 生成，改完重跑可复查）。

- [ ] **`推王龙门.md` 派系前提错误**（实质性，非笔误）— 官方术语表里格拉斯哥帮 = 推进之王、
      摩根、达格达、因陀罗，**不含维娜·维多利亚**。因此 `:32` 的推导「维娜和摩根都是格拉斯哥帮
      所以 +20%」不成立，戴菲恩只能给 +10%，面板 1.35 需重算。另：摩根「帮派指南针」的 +35%
      是专门给**推进之王**的。
- [x] ~~灵孑银崖喀兰效率 192% vs 129%~~ **已改为 129%**。
      按官方技能数据核算（银灰 +20%/上限+4、琳琅诗怀雅 订单分发·α +20% 与 招商引资 并存、
      灵知 对站内谢拉格 −15%/+6、孑 4%×上限）粗算约 132%，与 129% 只差基础 3% 与取整口径；
      192% 从任何读法都算不出（即使孑完全不压上限也只有约 148%）。
      加上文档内三处写 129、总览仅一处写 192，判定为数字转置笔误。

- [x] ~~`buff叠加模型.md:58` 关于孑的说法不成立~~ **误报，语料是对的，不用改。**
      原文「精 1 的市井之道多压了上限，又在后面**少了**摊贩经济的差值效率」——
      这里的「少了」指**收益变少**，不是技能被替换掉。两个技能确实并存
      （摊贩经济 `trade_ord_limit_diff_000` 初始解锁、市井之道 `trade_ord_limit_count_000`
      精英 1 解锁，族名不同），而市井之道压缩上限恰好会让摊贩经济的
      「上限 − 当前订单数」差值变小，从而削掉它自己的收益。这正是叠加的后果。

      完整机制（可作为将来补充语料的素材）：
      - 精 0（仅摊贩经济）：`4% × (上限 − 当前订单数)`，上限未被压缩。
        刚收完订单峰值高、订单堆满归零，**波动大**
      - 精 1（两技能并存）：两个子句互补（一个按差值、一个按当前订单数），
        合计恒为 `4% × 压缩后上限`，**恒定但天花板被自己压低**

      所以「没有灵知配合时精 0 孑可能比精 1 更实用」成立：没有灵知的 −15%
      削减他人效率，市井之道压得更狠，精 1 的恒定值被压得很低。
- [ ] **企鹅物流能蕾效率** — 同篇内 `:38` 算出面板 1.05，`:76` 数值锚点写「~80%」
- [ ] **两处对「赤金虚拟产线放进巫恋核」说法相反**（第二轮评测由模型独立发现）——
      - `巫恋裁缝核.md:52`：「赤金虚拟产线（鸿雪、绮良）…**归零后效率消失，等于白占一个位置**」
      - `buff叠加模型.md:73`：「赤金虚拟产线的干员（鸿雪、绮良等）放在巫恋核中**不受归零影响**」

      **按官方技能原文可以判定：`巫恋裁缝核.md:52` 对。**关键在排除条款的有无——
      - 巫恋「低语」：「当前贸易站内其他干员提供的订单获取效率**全部归零**」，**无排除条款**
      - 温蒂「仿生海龙」：「…生产力全部归零**（不包含根据设施数量提供加成的生产力）**」，**有排除条款**

      鸿雪「销路宣发」提供的是订单获取效率，落在低语的清零范围内。
      所以「清流不受温蒂影响」这条逻辑**不能类推到巫恋房**——两个归零技能的范围定义不同。
      建议删掉 `buff叠加模型.md:73`，或改写为「注意巫恋的归零没有设施数量排除条款，
      与温蒂不同」。

- [ ] **同效干员漏收 24 组** — 效果描述逐字相同、语料收了一部分漏了其余，
      是否补进 `散件干员速查.md`（也可能是有意省略低价值的）
- [ ] **迷迭香降级档 / 龙门中枢组** — `体系总览.md` 列了但无独立文档

### 首轮评测暴露的两处

- [x] ~~基建技能是叠加还是替换？~~ **已解决，不需要公孙处理**。
      游戏前端把每个基建技能标为「初始解锁 / 精英 N 解锁 / 精英 N 提升」——
      **提升 = 替换前一档，解锁 = 新增并存**。这个区分在两个本地数据源里都丢了
      （arkntools 上游已把 `buffChar[].buffData[]` 的槽位嵌套拍平，其 `index` 只是数组下标；
      RhodeLogisticsSteward 的抽取脚本同样两层循环拍平），但可以从 **buffId 族名**还原：
      同一干员名下同族且 ≥2 条时，除首条外都是提升。
      **判定规则与 `RIIC-Web/src/operatorPortraits.ts` 的 `isBuildingSkillEnhanced()` 相同**，
      已全库 913 条逐条比对，提升 167 条、零差异。`build_refs.py` 已实现，
      `references/技能-*.md` 逐条标注，文案与 RIIC-Web 干员查询页对齐
      （初始解锁 / 精英 N 解锁 / 等级 N 解锁 / 精英 N 提升）。
      全库：提升 167 条、解锁 746 条，165 / 425 名干员存在升级链。

      **给 KnightCode 的提醒**：`RhodeLogisticsSteward/scripts/extract_character_identity.py:36-37`
      把 `buffChar[]` 与 `buffData[]` 拍平成一维列表，`Skill` 数据类里也没有槽位字段。
      若求解器按列表求和，温蒂会被算成 +10% 与 +15% 叠加。值得自查一下
      `synergy/conflicts.py` 是否已经硬编码规避了这类情况。

- [ ] **`制造站机制.md` 把同一族技能拆进了两个互斥分类**（小问题，非知识缺失）——
      官方数据里 `manu_prod_spd&power_000/010/020` 是一条升级链，三个档位效果结构完全相同：
      「归零同房他人 + 每电站 +5%/+10%/+15%」。但语料把它们分到了两节：
      - `:88`「设施数量依赖型」下的 温蒂「仿生海龙」——只写 +15%/电站，**没提归零**
      - `:111`「归零他人类」下的「自动化 α/β」——写了归零，但**不含仿生海龙**

      单看这两节会以为温蒂精 2 是纯加成技能，可以和红云/泡泡这类高生产力干员叠加。
      不过 `自动化组.md:47,54` 已经把清零和「清流靠设施计数所以不受影响」讲清楚了，
      所以信息并不缺，只是分类错位 + 跨文档断链（与物流链那处同一性质）。

      建议改法：`:88` 补一句清零；`:111` 的列表加入仿生海龙。
      或者干脆把三个档位合并进「归零他人类」——它们本来就是一族。

## 已失效的待办

- [ ] ~~补全 `docs/5-cli求解器/`~~ — 求解器方案已废弃，改为引导用户到
      **可露希尔基建终端工具** https://riic.autos/ 。skill 侧已按此实现，本条可删。

---

## 待确认 / 后续

- [x] `办公室机制.md` — 焰狐龙梓兰数据已确认（联络速度+10%），已同步到散件干员速查
- [x] 模板编号修正：`tpl-基础设施.md` 中 `3-策略/` → `0-规则/`
- [ ] 考虑补 `base_systems.json` 数据文件（模板 `tpl-体系论证.md` 引用）
